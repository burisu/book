-# Auto-generated by rake :scaff
=simple_fields_for @person do |f|
  =fieldset :general do
    =f.input :patronymic_name, :required => true
    =f.input :family_name, :required => true
    =f.input :first_name, :required => true
    =f.input :second_name
    =f.input :user_name, :required => true
    =f.input :photo_file_name
    =f.input :sex, :required => true
    =f.input :born_on, :required => true
    =f.input :email, :required => true
    =f.input :rotex_email
    =f.input :student, :required => true, :collection => [['Oui', 1], ['Non', 0]], :as => :radio
    =f.input :comment, :input_html => {:rows => 5, :cols => 60}
    -person_promotions = Promotion.all
    -if person_promotions.count > 7
      =f.association :promotion, :collection => person_promotions, :as => :select
    -else
      =f.association :promotion, :collection => person_promotions, :as => :radio_buttons
    =f.input :language, :collection => countries, :as => :select
    =f.input :birth_country, :collection => countries, :as => :select
    =f.input :photo_file_size
    =f.input :photo_content_type
    =f.input :photo_updated_at
    -person_activities = Activity.all
    -if person_activities.count > 7
      =f.association :activity, :collection => person_activities, :as => :select
    -else
      =f.association :activity, :collection => person_activities, :as => :radio_buttons
    -person_organigram_professions = OrganigramProfession.all
    -if person_organigram_professions.count > 7
      =f.association :profession, :collection => person_organigram_professions, :as => :select
    -else
      =f.association :profession, :collection => person_organigram_professions, :as => :radio_buttons
