-# Auto-generated by rake :scaff
=toolbar do |t|
  -t.link :index
  -t.edit @organigram
  -t.destroy @organigram, :method => :delete, :confirm => 'Êtes-vous sûr(e) de vouloir supprimer cet enregistrement'
=fieldset :general do
  =attributes_list @organigram do |l|
    -l.attribute :name
    -l.attribute :code
    -l.attribute :description
=fieldset :meta do
  =attributes_list @organigram do |l|
    -l.attribute :id
    -l.attribute :created_at
    -l.attribute :updated_at
    -l.attribute :lock_version

%h2=tg(:x_professions, :count => @organigram.professions.count, :default => Organigram.human_attribute_name('professions'))
=toolbar do |t|
  -t.link :new, :controller => :organigram_professions, :organigram_id => @organigram.id, :redirect=>request.url
-if @organigram.professions.count > 0
  =list(:professions)
