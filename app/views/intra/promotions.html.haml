%h1=@promotion ? 'Promotion '+@promotion.name : 'Promotions'

.aclist
  -form_tag do
    =select_tag 'promotion', options_for_select(Promotion.find(:all, :order=>"name DESC").collect{|x| [x.name, x.id]}.insert(0,['','']), (@promotion ? @promotion.id : nil))
    =submit_tag 'Afficher la promotion'

=dyta :promotion_people
