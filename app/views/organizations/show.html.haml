-# Auto-generated by rake :scaff
=toolbar do |t|
  -t.link :index
  -t.edit @organization
  -t.destroy @organization, :method => :delete, :confirm => 'Êtes-vous sûr(e) de vouloir supprimer cet enregistrement'
=fieldset :general do
  =attributes_list @organization do |l|
    -l.attribute :name
    -l.attribute :description
    -l.attribute :comment
=fieldset :meta do
  =attributes_list @organization do |l|
    -l.attribute :id
    -l.attribute :created_at
    -l.attribute :updated_at
    -l.attribute :lock_version

%h2=tg(:x_group_natures, :count => @organization.group_natures.count, :default => Organization.human_attribute_name('group_natures'))
=toolbar do |t|
  -t.link :new, :controller => :group_natures, :organization_id => @organization.id, :redirect=>request.url
-if @organization.group_natures.count > 0
  =list(:group_natures)

%h2=tg(:x_group_kinships, :count => @organization.group_kinships.count, :default => Organization.human_attribute_name('group_kinships'))
=toolbar do |t|
  -t.link :new, :controller => :group_kinships, :organization_id => @organization.id, :redirect=>request.url
-if @organization.group_kinships.count > 0
  =list(:group_kinships)
