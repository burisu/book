-# Auto-generated by rake :scaff
=simple_fields_for @person_honour do |f|
  =fieldset :general do
    -person_honour_people = Person.all
    -if person_honour_people.count > 7
      =f.association :person, :collection => person_honour_people, :as => :select, :required => true
    -else
      =f.association :person, :collection => person_honour_people, :as => :radio_buttons, :required => true
    -person_honour_honours = Honour.all
    -if person_honour_honours.count > 7
      =f.association :honour, :collection => person_honour_honours, :as => :select, :required => true
    -else
      =f.association :honour, :collection => person_honour_honours, :as => :radio_buttons, :required => true
    =f.input :given_on
    =f.input :comment, :input_html => {:rows => 5, :cols => 60}
