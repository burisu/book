-# Auto-generated by rake :scaff
=toolbar do |t|
  -if @person_honour.person
    -t.show @person_honour.person
  -if @person_honour.honour
    -t.show @person_honour.honour
  -t.edit @person_honour
  -t.destroy @person_honour, :method => :delete, :confirm => 'Êtes-vous sûr(e) de vouloir supprimer cet enregistrement'
=fieldset :general do
  =attributes_list @person_honour do |l|
    -l.attribute :person, :label => :label, :url => true
    -l.attribute :honour, :label => :name, :url => true
    -l.attribute :given_on
    -l.attribute :comment
=fieldset :meta do
  =attributes_list @person_honour do |l|
    -l.attribute :id
    -l.attribute :created_at
    -l.attribute :updated_at
    -l.attribute :lock_version
