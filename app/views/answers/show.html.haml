-# Auto-generated by rake :scaff
=toolbar do |t|
  -if @answer.question
    -t.show @answer.question
  -t.edit @answer
  -t.destroy @answer, :method => :delete, :confirm => 'Êtes-vous sûr(e) de vouloir supprimer cet enregistrement'
=fieldset :general do
  =attributes_list @answer do |l|
    -l.attribute :created_on
    -l.attribute :ready
    -l.attribute :locked
    -l.attribute :person, :label => :label, :url => true
    -l.attribute :question, :label => :name, :url => true
=fieldset :meta do
  =attributes_list @answer do |l|
    -l.attribute :id
    -l.attribute :created_at
    -l.attribute :updated_at
    -l.attribute :lock_version

%h2=tg(:x_items, :count => @answer.items.count, :default => Answer.human_attribute_name('items'))
=toolbar do |t|
  -t.link :new, :controller => :answer_items, :answer_id => @answer.id, :redirect=>request.url
-if @answer.items.count > 0
  =list(:items)
