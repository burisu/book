-# Auto-generated by rake :scaff
=toolbar do |t|
  -t.link :index
  -t.edit @event
  -t.destroy @event, :method => :delete, :confirm => 'Êtes-vous sûr(e) de vouloir supprimer cet enregistrement'
=fieldset :general do
  =attributes_list @event do |l|
    -l.attribute :name
    -l.attribute :place
    -l.attribute :description
    -l.attribute :comment
    -l.attribute :started_at
    -l.attribute :stopped_at
    -l.attribute :nature, :label => :name, :url => true
=fieldset :meta do
  =attributes_list @event do |l|
    -l.attribute :id
    -l.attribute :created_at
    -l.attribute :updated_at
    -l.attribute :lock_version
