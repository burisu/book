-# Auto-generated by rake :scaff
=simple_fields_for @guest do |f|
  =fieldset :general do
    -guest_sale_lines = SaleLine.all
    -if guest_sale_lines.count > 7
      =f.association :sale_line, :collection => guest_sale_lines, :as => :select
    -else
      =f.association :sale_line, :collection => guest_sale_lines, :as => :radio_buttons
    -guest_sales = Sale.all
    -if guest_sales.count > 7
      =f.association :sale, :collection => guest_sales, :as => :select
    -else
      =f.association :sale, :collection => guest_sales, :as => :radio_buttons
    -guest_products = Product.all
    -if guest_products.count > 7
      =f.association :product, :collection => guest_products, :as => :select
    -else
      =f.association :product, :collection => guest_products, :as => :radio_buttons
    =f.input :first_name
    =f.input :last_name
    =f.input :email
    -guest_groups = Group.all
    -if guest_groups.count > 7
      =f.association :zone, :collection => guest_groups, :as => :select
    -else
      =f.association :zone, :collection => guest_groups, :as => :radio_buttons
    =f.input :annotation, :input_html => {:rows => 5, :cols => 60}
