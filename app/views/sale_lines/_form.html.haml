-# Auto-generated by rake :scaff
=simple_fields_for @sale_line do |f|
  =fieldset :general do
    -sale_line_sales = Sale.all
    -if sale_line_sales.count > 7
      =f.association :sale, :collection => sale_line_sales, :as => :select, :required => true
    -else
      =f.association :sale, :collection => sale_line_sales, :as => :radio_buttons, :required => true
    -sale_line_products = Product.all
    -if sale_line_products.count > 7
      =f.association :product, :collection => sale_line_products, :as => :select, :required => true
    -else
      =f.association :product, :collection => sale_line_products, :as => :radio_buttons, :required => true
    =f.input :name, :required => true
    =f.input :description, :input_html => {:rows => 5, :cols => 60}
    =f.input :unit_amount, :required => true
    =f.input :quantity, :required => true
    =f.input :amount, :required => true
