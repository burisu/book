-# Auto-generated by rake :scaff
=toolbar do |t|
  -if @sale_line.sale
    -t.show @sale_line.sale
  -t.edit @sale_line
  -t.destroy @sale_line, :method => :delete, :confirm => 'Êtes-vous sûr(e) de vouloir supprimer cet enregistrement'
=fieldset :general do
  =attributes_list @sale_line do |l|
    -l.attribute :sale, :label => :number, :url => true
    -l.attribute :product, :label => :name, :url => true
    -l.attribute :name
    -l.attribute :description
    -l.attribute :unit_amount
    -l.attribute :quantity
    -l.attribute :amount
=fieldset :meta do
  =attributes_list @sale_line do |l|
    -l.attribute :id
    -l.attribute :created_at
    -l.attribute :updated_at
    -l.attribute :lock_version

%h2=tg(:x_guests, :count => @sale_line.guests.count, :default => SaleLine.human_attribute_name('guests'))
=toolbar do |t|
  -t.link :new, :controller => :guests, :sale_line_id => @sale_line.id, :redirect=>request.url
-if @sale_line.guests.count > 0
  =list(:guests)
