-# Auto-generated by rake :scaff
=toolbar do |t|
  -t.link :index
  -t.edit @honour_nature
  -t.destroy @honour_nature, :method => :delete, :confirm => 'Êtes-vous sûr(e) de vouloir supprimer cet enregistrement'
=fieldset :general do
  =attributes_list @honour_nature do |l|
    -l.attribute :name
    -l.attribute :code
    -l.attribute :description
    -l.attribute :comment
=fieldset :meta do
  =attributes_list @honour_nature do |l|
    -l.attribute :id
    -l.attribute :created_at
    -l.attribute :updated_at
    -l.attribute :lock_version

%h2=tg(:x_honours, :count => @honour_nature.honours.count, :default => HonourNature.human_attribute_name('honours'))
=toolbar do |t|
  -t.link :new, :controller => :honours, :honour_nature_id => @honour_nature.id, :redirect=>request.url
-if @honour_nature.honours.count > 0
  =list(:honours)
