-# Auto-generated by rake :scaff
=toolbar do |t|
  -t.link :index
  -t.edit @sector
  -t.destroy @sector, :method => :delete, :confirm => 'Êtes-vous sûr(e) de vouloir supprimer cet enregistrement'
=fieldset :general do
  =attributes_list @sector do |l|
    -l.attribute :name
    -l.attribute :code
    -l.attribute :description
=fieldset :meta do
  =attributes_list @sector do |l|
    -l.attribute :id
    -l.attribute :created_at
    -l.attribute :updated_at
    -l.attribute :lock_version

%h2=tg(:x_activities, :count => @sector.activities.count, :default => Sector.human_attribute_name('activities'))
=toolbar do |t|
  -t.link :new, :controller => :activities, :sector_id => @sector.id, :redirect=>request.url
-if @sector.activities.count > 0
  =list(:activities)
