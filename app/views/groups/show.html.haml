-# Auto-generated by rake :scaff
=fieldset :general do
  =attributes_list do |l|
    -l.attribute :name
    -l.attribute :number
    -l.attribute :code
    -l.attribute :zone_nature, :label => :name, :url => true
    -l.attribute :parent, :label => :name, :url => true
    -l.attribute :country
=fieldset :meta do
  =attributes_list do |l|
    -l.attribute :id
    -l.attribute :created_at
    -l.attribute :updated_at
    -l.attribute :lock_version

%h2=tg(:x_interventions, :count => @group.interventions.count, :default => Group.human_attribute_name('interventions'))
=toolbar do |t|
  -t.link :new, :controller => :group_interventions, :group_id => @group.id
=list(:interventions)
