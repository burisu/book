-# Auto-generated by rake :scaff
=toolbar do |t|
  -t.link :index
  -t.edit @group_nature
  -t.destroy @group_nature, :method => :delete, :confirm => 'Êtes-vous sûr(e) de vouloir supprimer cet enregistrement'
=fieldset :general do
  =attributes_list @group_nature do |l|
    -l.attribute :name
    -l.attribute :organization, :label => :name, :url => true
    -l.attribute :zone_nature, :label => :name, :url => true
=fieldset :meta do
  =attributes_list @group_nature do |l|
    -l.attribute :id
    -l.attribute :created_at
    -l.attribute :updated_at
    -l.attribute :lock_version

%h2=tg(:x_groups, :count => @group_nature.groups.count, :default => GroupNature.human_attribute_name('groups'))
=toolbar do |t|
  -t.link :new, :controller => :groups, :group_nature_id => @group_nature.id, :redirect=>request.url
-if @group_nature.groups.count > 0
  =list(:groups)
