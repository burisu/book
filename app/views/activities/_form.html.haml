-# Auto-generated by rake :scaff
=simple_fields_for @activity do |f|
  =fieldset :general do
    -activity_sectors = Sector.all
    -if activity_sectors.count > 7
      =f.association :sector, :collection => activity_sectors, :as => :select, :required => true
    -else
      =f.association :sector, :collection => activity_sectors, :as => :radio_buttons, :required => true
    =f.input :label
    =f.input :name
    =f.input :code
