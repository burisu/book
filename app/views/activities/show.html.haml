-# Auto-generated by rake :scaff
=toolbar do |t|
  -if @activity.sector
    -t.show @activity.sector
  -t.edit @activity
  -t.destroy @activity, :method => :delete, :confirm => 'Êtes-vous sûr(e) de vouloir supprimer cet enregistrement'
=fieldset :general do
  =attributes_list @activity do |l|
    -l.attribute :sector, :label => :name, :url => true
    -l.attribute :label
    -l.attribute :name
    -l.attribute :code
=fieldset :meta do
  =attributes_list @activity do |l|
    -l.attribute :id
    -l.attribute :created_at
    -l.attribute :updated_at
    -l.attribute :lock_version
