-# Auto-generated by rake :scaff
=simple_fields_for @answer_item do |f|
  =fieldset :general do
    =f.input :content, :input_html => {:rows => 5, :cols => 60}
    -answer_item_answers = Answer.all
    -if answer_item_answers.count > 7
      =f.association :answer, :collection => answer_item_answers, :as => :select, :required => true
    -else
      =f.association :answer, :collection => answer_item_answers, :as => :radio_buttons, :required => true
    -answer_item_question_items = QuestionItem.all
    -if answer_item_question_items.count > 7
      =f.association :question_item, :collection => answer_item_question_items, :as => :select, :required => true
    -else
      =f.association :question_item, :collection => answer_item_question_items, :as => :radio_buttons, :required => true
