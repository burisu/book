-# Auto-generated by rake :scaff
=toolbar do |t|
  -if @answer_item.answer
    -t.show @answer_item.answer
  -t.edit @answer_item
  -t.destroy @answer_item, :method => :delete, :confirm => 'Êtes-vous sûr(e) de vouloir supprimer cet enregistrement'
=fieldset :general do
  =attributes_list @answer_item do |l|
    -l.attribute :content
    -l.attribute :answer, :label => :id, :url => true
    -l.attribute :question_item, :label => :name, :url => true
=fieldset :meta do
  =attributes_list @answer_item do |l|
    -l.attribute :id
    -l.attribute :created_at
    -l.attribute :updated_at
    -l.attribute :lock_version
