-# Auto-generated by rake :scaff
=toolbar do |t|
  -if @honour.nature
    -t.show @honour.nature
  -t.edit @honour
  -t.destroy @honour, :method => :delete, :confirm => 'Êtes-vous sûr(e) de vouloir supprimer cet enregistrement'
=fieldset :general do
  =attributes_list @honour do |l|
    -l.attribute :nature, :label => :name, :url => true
    -l.attribute :name
    -l.attribute :code
    -l.attribute :abbreviation
    -l.attribute :position
=fieldset :meta do
  =attributes_list @honour do |l|
    -l.attribute :id
    -l.attribute :created_at
    -l.attribute :updated_at
    -l.attribute :lock_version
