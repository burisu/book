-# Auto-generated by rake :scaff
=toolbar do |t|
  -if @period.person
    -t.show @period.person
  -t.edit @period
  -t.destroy @period, :method => :delete, :confirm => 'Êtes-vous sûr(e) de vouloir supprimer cet enregistrement'
=fieldset :general do
  =attributes_list @period do |l|
    -l.attribute :begun_on
    -l.attribute :finished_on
    -l.attribute :person, :label => :label, :url => true
    -l.attribute :comment
    -l.attribute :family_name
    -l.attribute :address
    -l.attribute :latitude
    -l.attribute :longitude
    -l.attribute :photo
    -l.attribute :phone
    -l.attribute :fax
    -l.attribute :email
    -l.attribute :mobile
    -l.attribute :country
=fieldset :meta do
  =attributes_list @period do |l|
    -l.attribute :id
    -l.attribute :created_at
    -l.attribute :updated_at
    -l.attribute :lock_version
