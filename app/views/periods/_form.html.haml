-# Auto-generated by rake :scaff
=simple_fields_for @period do |f|
  =fieldset :general do
    =f.input :begun_on, :required => true
    =f.input :finished_on, :required => true
    -period_people = Person.all
    -if period_people.count > 7
      =f.association :person, :collection => period_people, :as => :select, :required => true
    -else
      =f.association :person, :collection => period_people, :as => :radio_buttons, :required => true
    =f.input :comment, :input_html => {:rows => 5, :cols => 60}
    =f.input :family_name, :required => true
    =f.input :address, :required => true
    =f.input :latitude
    =f.input :longitude
    =f.input :photo
    =f.input :phone
    =f.input :fax
    =f.input :email
    =f.input :mobile
    =f.input :country, :collection => countries, :as => :select
