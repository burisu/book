-# Auto-generated by rake :scaff
=toolbar do |t|
  -t.link :index
  -t.edit @event_nature
  -t.destroy @event_nature, :method => :delete, :confirm => 'Êtes-vous sûr(e) de vouloir supprimer cet enregistrement'
=fieldset :general do
  =attributes_list @event_nature do |l|
    -l.attribute :name
    -l.attribute :comment
=fieldset :meta do
  =attributes_list @event_nature do |l|
    -l.attribute :id
    -l.attribute :created_at
    -l.attribute :updated_at
    -l.attribute :lock_version

%h2=tg(:x_events, :count => @event_nature.events.count, :default => EventNature.human_attribute_name('events'))
=toolbar do |t|
  -t.link :new, :controller => :events, :nature_id => @event_nature.id, :redirect=>request.url
-if @event_nature.events.count > 0
  =list(:events)
