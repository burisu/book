-# Auto-generated by rake :scaff
=toolbar do |t|
  -if @mandate.person
    -t.show @mandate.person
  -t.edit @mandate
  -t.destroy @mandate, :method => :delete, :confirm => 'Êtes-vous sûr(e) de vouloir supprimer cet enregistrement'
=fieldset :general do
  =attributes_list @mandate do |l|
    -l.attribute :dont_expire
    -l.attribute :started_on
    -l.attribute :stopped_on
    -l.attribute :nature, :label => :name, :url => true
    -l.attribute :person, :label => :label, :url => true
    -l.attribute :group, :label => :name, :url => true
=fieldset :meta do
  =attributes_list @mandate do |l|
    -l.attribute :id
    -l.attribute :created_at
    -l.attribute :updated_at
    -l.attribute :lock_version
