-# Auto-generated by rake :scaff
=simple_fields_for @mandate do |f|
  =fieldset :general do
    =f.input :dont_expire, :required => true, :collection => [['Oui', 1], ['Non', 0]], :as => :radio
    =f.input :started_on, :required => true
    =f.input :stopped_on
    -mandate_mandate_natures = MandateNature.all
    -if mandate_mandate_natures.count > 7
      =f.association :nature, :collection => mandate_mandate_natures, :as => :select, :required => true
    -else
      =f.association :nature, :collection => mandate_mandate_natures, :as => :radio_buttons, :required => true
    -mandate_people = Person.all
    -if mandate_people.count > 7
      =f.association :person, :collection => mandate_people, :as => :select, :required => true
    -else
      =f.association :person, :collection => mandate_people, :as => :radio_buttons, :required => true
    -mandate_groups = Group.all
    -if mandate_groups.count > 7
      =f.association :group, :collection => mandate_groups, :as => :select
    -else
      =f.association :group, :collection => mandate_groups, :as => :radio_buttons
