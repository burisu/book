-# Auto-generated by rake :scaff
=toolbar do |t|
  -if @group_kinship.parent
    -t.show @group_kinship.parent
  -if @group_kinship.child
    -t.show @group_kinship.child
  -t.edit @group_kinship
  -t.destroy @group_kinship, :method => :delete, :confirm => 'Êtes-vous sûr(e) de vouloir supprimer cet enregistrement'
=fieldset :general do
  =attributes_list @group_kinship do |l|
    -l.attribute :parent, :label => :name, :url => true
    -l.attribute :child, :label => :name, :url => true
    -l.attribute :organization, :label => :name, :url => true
=fieldset :meta do
  =attributes_list @group_kinship do |l|
    -l.attribute :id
    -l.attribute :created_at
    -l.attribute :updated_at
    -l.attribute :lock_version
