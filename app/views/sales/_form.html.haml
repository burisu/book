-# Auto-generated by rake :scaff
=simple_fields_for @sale do |f|
  =fieldset :general do
    =f.input :number, :required => true
    =f.input :comment, :input_html => {:rows => 5, :cols => 60}
    -sale_people = Person.all
    -if sale_people.count > 7
      =f.association :client, :collection => sale_people, :as => :select
    -else
      =f.association :client, :collection => sale_people, :as => :radio_buttons
    =f.input :client_email, :required => true
    =f.input :amount, :required => true
    =f.input :payment_mode, :required => true
    =f.input :payment_number
