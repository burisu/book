-# Auto-generated by rake :scaff
=toolbar do |t|
  -t.link :index
  -t.edit @mandate_nature
  -t.destroy @mandate_nature, :method => :delete, :confirm => 'Êtes-vous sûr(e) de vouloir supprimer cet enregistrement'
=fieldset :general do
  =attributes_list @mandate_nature do |l|
    -l.attribute :name
    -l.attribute :rights
    -l.attribute :group_nature, :label => :name, :url => true
=fieldset :meta do
  =attributes_list @mandate_nature do |l|
    -l.attribute :id
    -l.attribute :created_at
    -l.attribute :updated_at
    -l.attribute :lock_version

%h2=tg(:x_mandates, :count => @mandate_nature.mandates.count, :default => MandateNature.human_attribute_name('mandates'))
=toolbar do |t|
  -t.link :new, :controller => :mandates, :nature_id => @mandate_nature.id, :redirect=>request.url
-if @mandate_nature.mandates.count > 0
  =list(:mandates)
