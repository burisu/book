-# Auto-generated by rake :scaff
=toolbar do |t|
  -if @question_item.question
    -t.show @question_item.question
  -t.edit @question_item
  -t.destroy @question_item, :method => :delete, :confirm => 'Êtes-vous sûr(e) de vouloir supprimer cet enregistrement'
=fieldset :general do
  =attributes_list @question_item do |l|
    -l.attribute :name
    -l.attribute :explanation
    -l.attribute :position
    -l.attribute :question, :label => :name, :url => true
    -l.attribute :theme, :label => :name, :url => true
=fieldset :meta do
  =attributes_list @question_item do |l|
    -l.attribute :id
    -l.attribute :created_at
    -l.attribute :updated_at
    -l.attribute :lock_version

%h2=tg(:x_answer_items, :count => @question_item.answer_items.count, :default => QuestionItem.human_attribute_name('answer_items'))
=toolbar do |t|
  -t.link :new, :controller => :answer_items, :question_item_id => @question_item.id, :redirect=>request.url
-if @question_item.answer_items.count > 0
  =list(:answer_items)
