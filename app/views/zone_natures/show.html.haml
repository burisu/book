-# Auto-generated by rake :scaff
=toolbar do |t|
  -t.link :index
  -t.edit @zone_nature
  -t.destroy @zone_nature, :method => :delete, :confirm => 'Êtes-vous sûr(e) de vouloir supprimer cet enregistrement'
=fieldset :general do
  =attributes_list @zone_nature do |l|
    -l.attribute :name
    -l.attribute :parent, :label => :name, :url => true
=fieldset :meta do
  =attributes_list @zone_nature do |l|
    -l.attribute :id
    -l.attribute :created_at
    -l.attribute :updated_at
    -l.attribute :lock_version

%h2=tg(:x_children, :count => @zone_nature.children.count, :default => ZoneNature.human_attribute_name('children'))
=toolbar do |t|
  -t.link :new, :controller => :zone_natures, :parent_id => @zone_nature.id, :redirect=>request.url
-if @zone_nature.children.count > 0
  =list(:children)

%h2=tg(:x_groups, :count => @zone_nature.groups.count, :default => ZoneNature.human_attribute_name('groups'))
=toolbar do |t|
  -t.link :new, :controller => :groups, :zone_nature_id => @zone_nature.id, :redirect=>request.url
-if @zone_nature.groups.count > 0
  =list(:groups)
