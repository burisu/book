-# Auto-generated by rake :scaff
=fieldset :general do
  =attributes_list do |l|
    -l.attribute :name
    -l.attribute :code
    -l.attribute :logo
    -l.attribute :description
    -l.attribute :parent, :label => :name, :url => true
    -l.attribute :rubrics_count
=fieldset :meta do
  =attributes_list do |l|
    -l.attribute :id
    -l.attribute :created_at
    -l.attribute :updated_at
    -l.attribute :lock_version

%h2=tg(:x_articles, :count => @rubric.articles.count, :default => Rubric.human_attribute_name('articles'))
=toolbar do |t|
  -t.link :new, :controller => :articles, :rubric_id => @rubric.id
=list(:articles)
