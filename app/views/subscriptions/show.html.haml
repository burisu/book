-# Auto-generated by rake :scaff
=toolbar do |t|
  -t.link :index
  -t.edit @subscription
  -t.destroy @subscription, :method => :delete, :confirm => 'Êtes-vous sûr(e) de vouloir supprimer cet enregistrement'
=fieldset :general do
  =attributes_list @subscription do |l|
    -l.attribute :begun_on
    -l.attribute :finished_on
    -l.attribute :person, :label => :label, :url => true
    -l.attribute :number
    -l.attribute :sale, :label => :number, :url => true
    -l.attribute :sale_line, :label => :name, :url => true
=fieldset :meta do
  =attributes_list @subscription do |l|
    -l.attribute :id
    -l.attribute :created_at
    -l.attribute :updated_at
    -l.attribute :lock_version
