-# Auto-generated by rake :scaff
=toolbar do |t|
  -t.link :index
  -t.edit @person_contact_nature
  -t.destroy @person_contact_nature, :method => :delete, :confirm => 'Êtes-vous sûr(e) de vouloir supprimer cet enregistrement'
=fieldset :general do
  =attributes_list @person_contact_nature do |l|
    -l.attribute :name
    -l.attribute :canal
=fieldset :meta do
  =attributes_list @person_contact_nature do |l|
    -l.attribute :id
    -l.attribute :created_at
    -l.attribute :updated_at
    -l.attribute :lock_version

%h2=tg(:x_person_contacts, :count => @person_contact_nature.person_contacts.count, :default => PersonContactNature.human_attribute_name('person_contacts'))
=toolbar do |t|
  -t.link :new, :controller => :person_contacts, :person_contact_nature_id => @person_contact_nature.id, :redirect=>request.url
-if @person_contact_nature.person_contacts.count > 0
  =list(:person_contacts)
